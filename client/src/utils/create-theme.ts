import { createTheme } from '@material-ui/core';

export const themeMUI = createTheme({
  palette: {
    primary: {
      main: '#48A9A6',
    },
    secondary: {
      main: '#D4B483',
    },
  },
});
